Libraries: stdio.h, stdlib.h, time.h #We'll use random functions for the lower-level AI

Since a connect-4 board is 6x7, we'll  use #define ROWS 6 and #define COLUMNS 7
We'll also need an array for the board

Our starting file will look like this:

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define ROWS 6
#define COLUMNS 7

int main(){
  char board[ROWS][COLUMNS]; /* Using 'r', 'y', and 'n' to represent the circles */

  #Keeps the game going and interactions between functions
  #Author: Ethan

  return 0;
}

void printBoard(*char board[ROWS][COLUMNS]){
  #Print a stylized board to the terminal w/ colored text
  #Author: Ethan
}

int checkLinks(*char board[ROWS][COLUMNS], int linkLength){
  #Could you instead change the code to check for a link of length linkLength instead of just 4?
  #This could be useful for the AI to check if the player is close to winning.
  #Return int array { {(link 1) {row1, column1}, {row2, column2}}, {(link 2) {row1, column1}, {row2, column2}} }
  #Let main or aiMove decide what color the links are after the call
  #Author: Ryan
}

int makeMove(*char board[ROWS][COLUMNS], int column){
  #Check if the column entered is valid, and if so, what row the new circle would be on
  #Return -1 if it's an invalid row
  #Author: Ryan
}

int aiMove(*char board[ROWS][COLUMNS], int difficulty){
  #The ai attempts to make a move based on the difficulty
  #Easy (0): AI makes random moves
  #Medium (1): AI will try to prevent you from winning if you have a 3-link
  #Hard (2): Simulates future boards and tries to find the best position
  #Wizard (3) (optional): can place circles in places they're not supposed to be (requires new makeMove function)

  #Author: Carson
}
